document.addEventListener("DOMContentLoaded",()=>{const b=document.querySelector(".menu-toggle"),m=document.querySelector(".site-nav");if(b&&m)b.addEventListener("click",()=>{const o=m.classList.toggle("open");b.setAttribute("aria-expanded",String(o))});const modal=document.querySelector(".gallery-modal"),content=document.querySelector(".gallery-dialog-content"),close=document.querySelector(".modal-close");document.querySelectorAll(".project-image").forEach(item=>item.addEventListener("click",()=>{if(!modal||!content)return;const src=item.getAttribute("data-full"),label=item.getAttribute("data-label")||"Project image";content.innerHTML=src?'<img src="'+src+'" alt="'+label.replace(/"/g,"&quot;")+'">':'<div class="placeholder-modal">Project photos coming soon</div>';modal.classList.add("open")}));const closeModal=()=>modal&&modal.classList.remove("open");if(close)close.addEventListener("click",closeModal);if(modal)modal.addEventListener("click",e=>{if(e.target===modal)closeModal()});document.addEventListener("keydown",e=>{if(e.key==="Escape")closeModal()})});